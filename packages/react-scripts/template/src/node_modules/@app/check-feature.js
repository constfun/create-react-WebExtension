// Check if a module exists at src/[featureName]
export default function (featureName) {
    try {
        require.resolve(`../../${featureName}`);
        return true;
    }
    catch (_) {
        return false;
    }
}